NODE_ENV=development
PORT=5000

# Database (SQLite is default, so no DB_HOST needed)
DB_PATH=./database/database.sqlite

# JWT
JWT_SECRET=your_super_secret_jwt_key_32chars
JWT_EXPIRES_IN=7d

# Frontend URL (Point to your Vercel URL in production)
CLIENT_URL=http://localhost:3000
